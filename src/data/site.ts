/**
 * Constantes transverses du site (hors données des 3 maisons, qui vivent dans
 * `maisons.ts`). Source unique pour l'adresse de contact publique : un
 * changement d'adresse ne touche qu'ici + les textes légaux des dictionnaires.
 */
export const CONTACT_EMAIL = "contact@olea-restaurant.fr";
export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}`;
